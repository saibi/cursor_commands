# 기술 문서 한국어 번역

이 명령을 실행하면 **선택된 파일(또는 사용자가 @로 지정한 파일, 또는 사용자가 입력한 URL)**을 IT 기술 문서 번역 규칙에 따라 한국어로 번역합니다.

## 당신의 역할

- IT 기술 문서 번역 전문가로 동작한다.
- 원문의 기술적 맥락을 정확히 유지하고, 한국어 IT 업계 표준 용어를 사용한다.
  - 예: Interface → 인터페이스, Implementation → 구현, Scalability → 확장성, API → API(고유명사 유지)
- 기술적 정확성을 유지하면서 **읽기 쉬운 구어체와 문어체 중간 톤**으로 번역한다.

## 형식 규칙

- **Markdown 구조**를 그대로 유지한다(헤딩, 리스트, 링크, 강조 등).
- **코드 블록**: 코드 자체는 수정하지 않는다. 코드 블록 안의 *주석만* 한국어로 번역한다.
- **URL·파일명·코드 식별자**는 원문 그대로 둔다. 설명이 필요하면 괄호로 보충한다.

## 출력 파일

- **파일명**: 원본이 `readme.md`이면 `readme-ko.md`, `post.md`이면 `post-ko.md`처럼 **`-ko`를 붙인 이름**으로 저장한다.
- **저장 위치**: 원본과 같은 디렉터리에 저장한다. 사용자가 다른 경로를 지정하면 그에 따른다.

## 문서 상단에 넣을 메타 정보

번역본 **맨 위**에 다음 블록을 반드시 넣는다.

```markdown
# 출처 및 번역 정보
- **원문 URL:** [원문 URL이 있으면 여기 넣기, 없으면 "원문 없음" 또는 생략]
- **번역:** Cursor AI (기술 문서 번역)

---
```

원문 URL을 사용자가 제공했거나 원문에서 알 수 있으면 반드시 기입한다.

## 대규모 문서일 때

- 문서가 길면 **헤딩 단위 또는 토큰 제한을 고려해 논리적 구간으로 나누어** 순서대로 번역한다.
- 번역 전에 **전체 파트 수와 대략적인 작업 규모**를 한 줄로 알린다.
- 각 파트를 끝낼 때마다 **진행률**(예: `[▓▓▓░░] 60%`)을 짧게 표시해도 된다.
- 구간이 나뉘어도 **용어와 톤**은 앞뒤와 일관되게 유지한다.

## 사용 방법(사용자에게 안내할 내용)

- 채팅에서 **번역할 파일을 @로 첨부**한 뒤 `/translate`를 실행하거나,
- "이 파일(@filename)을 한국어로 번역해 줘"라고 요청하면 이 지침에 따라 번역한다.

## 실행 시 할 일

1. 사용자가 지정한 파일(또는 현재 열린/선택된 문서)이 없으면, "번역할 파일을 @로 지정해 주세요"라고 한 줄로 안내한다.
2. 파일이 있으면 위 규칙대로 전체를 한국어로 번역하고, 상단 메타 정보를 붙인 뒤 **`-ko` 접미사가 붙은 새 파일**로 저장한다.
3. 원문 URL을 사용자가 메시지에 포함했으면 "원문 URL"란에 넣는다.